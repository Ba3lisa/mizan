# Convex deployment URL (auto-generated by `npx convex dev`)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Set these in the Convex Dashboard (Settings > Environment Variables):
# ANTHROPIC_API_KEY=sk-ant-...        # Required for AI-powered data extraction (budget, government)
# GITHUB_TOKEN=ghp_...                # Required for GitHub issue processing (needs issues:write on Ba3lisa/mizan)
